Answers to your 5 questions – please proceed with these choices

Checkpoint Storage Format

Go with Option B:

Store lightweight JSON metadata + measurement counts / last timestamp, not full ZIP blobs.

Use checkpoints mainly for:

Detecting that data is being written regularly.

Letting me see “last checkpoint at HH:MM, X measurements”.

When I need a real export ZIP, we’ll still generate it on demand with the existing export system.

So: no periodic ZIP blobs in IndexedDB, just checkpoint metadata + whatever you need to quickly generate a full export later.

Storage Health Warning Location

Use Option C: both.

Top bar banner when backlog / health is in a dangerous state (e.g. many pending writes, last successful write older than threshold). This must be obvious to a field user.

Developer/Debug panel for detailed info:

pendingWrites

lastSuccessfulWriteAt

maybe last checkpoint time and size.

So:

Critical = banner.

Details = debug panel.

Existing appendToLocalCSV Usage

Do this:

Keep the worker-based CSV append (it’s off-thread and fine).

Remove/disable the main-thread fallback usage in addMeasurement() where workers are not available.

In other words:

appendToLocalCSV must not run on the main thread per POI.

It can still be used inside the worker as a belt-and-suspenders backup.

So between your options: closest to Option A, but with the main-thread fallback removed. Main thread should never do per-POI CSV writes.

Test Location

Use Option A as the minimum requirement:

Add a Vitest test file that simulates high-load logging (e.g. 10,000 measurements at 5–10 POIs/s) using the real batching/ack logic.

The test should assert at the end that:

All measurements are acknowledged as saved.

pendingWrites === 0.

No errors are thrown.

If it’s easy later, you can also add a dev-only simulation button in the Developer panel, but the automated test is the priority.

Checkpoint Store / DB Version

Yes, I’m OK bumping the DB version from 15 → 16 to add the new surveyCheckpoints store.

Please:

Add the store in version 16.

Make sure existing data is preserved during the upgrade.

Document the new store in the DB schema comments.

Please proceed with implementation based on the above, including:

ACK-based pending write tracking + UI banner + debug details.

Removal of main-thread appendToLocalCSV usage (keep worker-based only).

10-minute checkpoint logic using JSON metadata in surveyCheckpoints.

Flush on visibilitychange / beforeunload.

A Vitest high-load test that validates the full pipeline.