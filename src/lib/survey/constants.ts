/**
 * Shared survey constants — import from here to avoid circular dependencies.
 */

/** Default number of POIs before an auto-split is triggered. */
export const DEFAULT_AUTO_PART_THRESHOLD = 200;
