// Stub — original deleted during orphan cleanup
export async function saveFileNative(_blob: Blob, _filename: string, _mimeType?: string): Promise<boolean> {
  return false;
}
export default saveFileNative;
