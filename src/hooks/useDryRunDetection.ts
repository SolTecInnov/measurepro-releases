// Stub — original deleted during orphan cleanup
export function useDryRunDetection(_config: any = {}) {
  return { isDetecting: false, detections: [], start: () => {}, stop: () => {}, reset: () => {} };
}
