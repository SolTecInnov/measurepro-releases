# Build Resources

Place the following icon files here before building:

- `icon.ico` — Windows icon (256x256 minimum)
- `icon.png` — Fallback icon for development

Generate icons from a single high-resolution PNG using electron-builder's icon tools or https://www.electron.build/icons.
